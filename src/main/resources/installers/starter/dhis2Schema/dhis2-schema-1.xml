<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">

    <changeSet id="20240426" author="victor">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(table_name) FROM information_schema.tables WHERE table_name = 'dhis2_uploads';
            </sqlCheck>
        </preConditions>
        <sql>
            CREATE TABLE dhis2_uploads (
                id BIGINT NOT NULL,
                filename VARCHAR(255),
                upload_date date,
                status VARCHAR(255),
                CONSTRAINT pk_dhis2_uploads PRIMARY KEY (id)
            );
        </sql>
    </changeSet>

    <changeSet id="20240427" author="victor">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(table_name) FROM information_schema.tables WHERE table_name = 'dhis2_configuration';
            </sqlCheck>
        </preConditions>
        <sql>
            CREATE TABLE dhis2_configuration (
                id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
                url VARCHAR(255),
                username VARCHAR(255),
                password VARCHAR(255),
                CONSTRAINT pk_dhis2_configuration PRIMARY KEY (id)
            );
        </sql>
    </changeSet>
</databaseChangeLog>